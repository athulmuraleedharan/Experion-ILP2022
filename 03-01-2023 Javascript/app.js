const student = {
    name: 'Mr.X',
    age: 19,
    mark1: 10,
    mark2: 12
}
const data = JSON.stringify(student);// json is used to convert object to string fro transfer to server or to write in document
console.log(data);

